'use client';

// This component is no longer needed as all controls are shown through the notification system
// If you have specific functionality that isn't covered by the notification system,
// please integrate it with the NotificationContext approach

export default function KeyboardControls() {
  return null;
}
